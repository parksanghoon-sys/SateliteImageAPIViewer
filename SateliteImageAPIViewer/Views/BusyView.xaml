<UserControl x:Class="SateliteImageAPIViewer.Views.BusyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SateliteImageAPIViewer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="#66000000">
            <Border
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="White"
                    CornerRadius="5">
                <StackPanel Width="100" Margin="10">
                    <ProgressBar Height="10" IsIndeterminate="True" Background="#F3F3D3">
                        <ProgressBar.Foreground>
                            <SolidColorBrush Color="#03C685"/>
                        </ProgressBar.Foreground>
                    </ProgressBar>
                    <TextBlock
                            Margin="0,5,0,0"
                            HorizontalAlignment="Center"
                            Text="Busy..." />
                </StackPanel>
                <Border.Effect>
                    <DropShadowEffect />
                </Border.Effect>
            </Border>
        </Border>
    </Grid>
</UserControl>
